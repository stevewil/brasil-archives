# Master Handoff — 2026-08-27

**Author:** agent session (steve@stevewil, `stevewil@gmail.com`)
**Purpose:** Bring the next agent up to speed on the entire Brasil Archives Project ecosystem — three repos, their relationships, deploy pipeline, active tracks, and the specific token-efficient tooling proposal for continuing UI polish work.

**Revised 2026-08-27 (later session):** brought §3/§5/§6/§9/§10 in sync with
`main` at `f983819`, the Windows-workstation checkout at
`c:\DEV\brasil-archives` (port 9000), and the two new root docs
`DESCRIPTION.md` + `TODO.md`. `TODO.md` is now the authoritative ordered
agenda; this file is the ecosystem orientation.

**Revised 2026-08-27 (UI-polish session):** all five UI-polish tracks
landed in one session (`891f30a`..`4f59d27`) — see §4. `main` is at
`4f59d27`, tests **170 passed / 4 skipped**, tree clean, everything pushed.
Tracks 2, 4, 5 are deployed and verified live; **Tracks 1 + 3 are pushed
but not yet pulled to cPanel** (Track 1's pull also needs
`pybabel compile -d app/translations` — `.mo` is git-ignored; see
`docs/DEPLOY.md`). `app.bat` (start/stop/restart/status) added. §3, §5,
§6, §9 updated below.

**Revised 2026-08-27 (parallel-jobs session):** four deferred large jobs
run as parallel worktree agents. **Landed + pushed to `main`** (`34fc5b3`,
259 passed / 4 skipped): the quarterly health-**probe runner** (`d2b808d`,
`scripts/probe.py`), the **OAI-PMH provider + EAG XML** at `/oai`
(`54367f7`), `docs/vocabularies.md`, and the `Archive.size_unit_note`
column (`e2c3d4f5a6b7`). Two new migrations —
`d7f1a2b3c4d5`, `e2c3d4f5a6b7`. Full record:
`docs/handoff/2026-08-27-parallel-jobs.md`.

**Revised 2026-08-28 (deploy + review session):** ran the deploy/review
runbook (`docs/handoff/2026-08-27-runbook.md`).
- **Phase 1** — deployed `main` to cPanel (now on `1cc5ded`); found + fixed
  a reseeded prod DB (re-ran `load_calibration` + `load_upgrade_projects`
  + `harvest`; prod back to 79 archives / 1016 records). `docs/DEPLOY.md`
  2026-08-28 note has the recovery sequence.
- **Phase 2** — probe robustness pass (`9563f47`): 90s per-target budget,
  Wayback/Semantic-Scholar soft-miss handling. Full prod probe run + the
  cron are still to do (cron lines in the runbook Phase 2).
- **Phase 3** — Pass 3 reviewed, merged, loaded (`b6eceee`). 21 archives
  now scored (6 Pass 2 + 15 Pass 3). All 8 borderline calls → option A;
  EAP703 confirmed live (`accessibility 8`); 5 archives now carry
  `scholarly_access_practical = only-via-federation`. cPanel deploy of
  Pass 3 (runbook 3c) still pending.
- **Phase 4** — EAG `repositoryType` map fixed (`bb044b4`); the rest keep/defer.
- **Phase 5 / 5.5** — povos `/oai` **and** federation-v1 `/api/*`
  (`app/federation.py`) built, merged, **deployed + verified**. povos `main`
  `466f8c1`.
- **Phase 6 — Track C done.** povos is brasil-archives **upgrade project #2**
  (`scripts/seed_povos_archive.py` +
  `configs/upgrade_projects/povos-indigenas-rn.yaml` +
  `tests/test_load_povos.py`, `8d81da0`). Live federation preview verified
  locally. Track B (povos OAI) also done.
- Local `c:\DEV\mipibu` pulled current (`3e00a55`, has `app/oai/`).

**Revised 2026-08-29 (operational + ADR session).** All runbook phases
deployed and verified. Then:
- **Prod probe run** — 82 targets (57 ok / 20 partial). 5 outright
  failures exposed + fixed 3 `http_get` bugs (`77a22ae`: junk/non-ASCII
  `canonical_url`, `ConnectionResetError`); re-probed clean.
- **Probe-fed facet display** (`3dbb3ac`) — the 4 probe facets +
  "Last checked" now render on the archive detail page ("Observed
  signals" section, PT+EN).
- **4 crons installed** on cPanel: quarterly probe, monthly harvest
  (mipibu `oai_dc` + `oai_ead`, povos `oai_dc`). Environment-tested via
  `sh -c`. First povos prod harvest done — prod `aggregated_records` = 1161.
- **[ADR-0002](../adr-0002-axis-re-examination.md) accepted** (`d9376ee`):
  re-examined the two-axis aggregation over all 21 scored archives. 4-4
  partition kept; **quadrant threshold 28 → 26** (medians are pipeline 24 /
  research 22); research axis found not internally coherent (Cronbach
  α 0.49) — `uniqueness` + `corpus_completeness` flagged for a Pass 4
  review. On the live site LABIM/UFRN + APEB finding-aid indexes move to
  "High research".
- **Fixes:** `federation.html_deep_link` per-partner (`c5fb1f8`, was
  hardcoded to mipibu's `/cases` → 404 for povos); OAI `Identify`
  `sampleIdentifier` (`0cf8c9f`, pointed at a non-existent slug →
  registration would fail); `github-pull` hardened (`5ab8cde`, no longer
  skips the Passenger restart when an optional step fails); povos
  `* text=auto` renormalize (`75801fe`).

**Revised 2026-08-29 (Phase 3.5 session).** Built **federated search** —
public `GET /search` over the harvested `oai_dc` records
(`app/services/federated_search.py`, `app/templates/search.html`,
`tests/test_federated_search.py`, 16 tests). Accent/case-insensitive full
scan, strong/weak match tiers, per-partner facet chips, host-matched deep
links back into each partner viewer, pagination, bilingual (new PT/EN
msgids — deploy needs `pybabel compile`). Nav gained a "Search" link; the
home "Federated records" stat links in. `TODO.md` §6 item + "next
session" item 7 closed. Design + follow-ups:
[`docs/federated-search.md`](../federated-search.md). Suite **288 passed /
4 skipped**.

`main` is at **`f5ada51`** + this session's commit (272→288 passed / 4
skipped); cPanel is one `github-pull` behind (carries the deep-link/
sample-id fixes, the probe display, the 28→26 threshold, **and now
federated search** — this pull touches `app/translations/**/*.po`, so the
deploy runs `pybabel compile -d app/translations`). povos `main`
`75801fe`, deployed at `466f8c1` + verified (the `75801fe` renormalize is
optional).

**Everything on the runbook is done bar two user-driven items:** the
`github-pull`, and the OAI-PMH registry submission (`docs/oai-pmh-provider.md`
§6 — set `OAI_PAGE_SIZE=50` env var first).

**Next session: a "toward 1.0" discussion.** What separates the current
state (a working federated catalog with 21 scored archives, 2 partners,
`/oai` + EAG, a live health probe) from a 1.0 release. Candidate topics:
the `LICENSE` / `LICENSING.md` deferral (blocks public release per
`README.md`), Pass 4 scoring coverage (21 of ~50 pipeline-viable), the
Scale digitized-vs-published question, `no_digital_content` / fatal-flaw
archives, and whether the two-axis model + probe thresholds are calibrated
enough to publish scores publicly. (Phase 3.5 cross-corpus search — built
2026-08-29, no longer a 1.0 gap.)

> **Read this first, then follow the links.** If you only read one document to pick up this project, this is it. Everything else is either a design doc (long-lived) or a pickup brief for a specific track.

---

## 1. Ecosystem overview

The Brasil Archives Project is a three-repo system:

| Repo | Role | Deploy target | Status |
|------|------|---------------|--------|
| **[stevewil/brasil-archives](https://github.com/stevewil/brasil-archives)** | Metadata catalog + scoring + federation aggregator | `https://brasil-archives.from-bottom-to.top` (live) | UI polish + `/oai`/EAG + quarterly probe + 2 partners federated + 21 archives scored. Pre-1.0 — see the "toward 1.0" note at the top. |
| **[stevewil/mipibu](https://github.com/stevewil/mipibu)** | São José de Mipibu judicial corpus explorer | `https://mipibu.from-bottom-to.top` (cPanel) | Live, OAI-PMH endpoint verified |
| **[stevewil/povos-indigenas-rn](https://github.com/stevewil/povos-indigenas-rn)** | Indigenous history of RN corpus explorer | `https://povos-indigenas-rn.from-bottom-to.top` (live) | Deployed; `/oai` merged (`817167c`), prod pull pending; Track C next |

**Relationship:**

- `brasil-archives` is the **aggregator**. Public-facing catalog of Brazilian digital archives with a scoring algorithm, and the federation counterpart that harvests from partner corpus explorers.
- `mipibu` and `povos-indigenas-rn` are **partner corpus explorers** — deep-dive apps for specific archives. They register with `brasil-archives` as "upgrade projects" and expose their content via a federation contract (JSON API + eventually OAI-PMH).
- The federation contract lives in [`brasil-archives/docs/federation-v1.md`](../federation-v1.md).
- `mipibu` is the reference implementation; `povos` is the second-caller that will trigger extraction of shared code (see §5).

## 2. Standing constraints (preserve verbatim)

These are non-negotiable across every session. They come from prior conversations with the user and multiple sessions have validated them.

- **Bilingual PT/EN throughout.** Everything user-facing must render in both.
- **National-period historian focus** (1800–1900). Age-as-value bias is **rejected**; a 1960s archive is not automatically less valuable than an 1860s one.
- **Nordeste focus.** RN, PE, BA are primary; ecclesiastical sources are important; resource-poor archives are supported, not filtered out.
- **Fair use / uso justo is the fundamental floor.** Every archive on the public site must clear this bar. Terminology: use "uso justo," not "uso legítimo."
- **`brasil-archives` = metadata of archives.** It is separate from `mipibu`-style corpus explorers. Don't mix corpus data into brasil-archives; don't mix archive catalog data into a corpus explorer.
- **Licensing:** MIT/Apache for code, CC-BY-SA for data.
- **Pull-only cPanel, never push.** All cPanel deploys pull from GitHub. Never SCP, rsync, or SFTP code up.
- **Wasabi keys stay on workstation only** — never sent over chat. All Wasabi buckets in `us-west-2`.
- **Design 2:** local is primary, Wasabi is distribution, cPanel is public mirror.
- **Cache TTL: 15 minutes hardcoded** (`CACHE_TTL_SECONDS = 900`).
- **Lazy fetch** on page render (not background refresh).

## 3. Current state (2026-08-27)

### brasil-archives

- **HEAD:** `f5ada51` on `main` (2026-08-29), clean, pushed. See the
  revision notes at the top for the full chain; `TODO.md` "Landed" section
  is the commit-level index.
- **Tests:** **272 passed, 4 skipped** (opt-in live).
- **Live URL:** `https://brasil-archives.from-bottom-to.top`. Schema at
  `e2c3d4f5a6b7` (2 migrations: `last_probed_at`, `size_unit_note`).
  cPanel is **one `github-pull` behind `main`** — carries the
  `html_deep_link`/OAI-sample-id fixes, the probe-facet display, and the
  ADR-0002 28→26 quadrant threshold.
- **Prod DB:** 80 archives (79 survey + `povos-indigenas-rn-corpus`), **2
  upgrade projects** (mipibu, povos), **1161 aggregated records** (mipibu
  508 `oai_dc` + 508 `oai_ead`, povos 145 `oai_dc`), 21 archives fully
  scored, every archive probed once (`probe_results`). **The prod SQLite DB
  had been reseeded** before 2026-08-28 (lost scores/facets/tags, harvest
  tables, mipibu's OAI config) — restored via
  `load_calibration` + `load_upgrade_projects` + `seed_povos_archive` +
  `harvest`. **After any future reseed, re-run that recovery sequence** —
  full list in `docs/DEPLOY.md` "2026-08-28 note".
- **Local workstation checkout:** `c:\DEV\brasil-archives` (Windows, the
  user's primary per "Design 2: local is primary"). `.venv` exists
  (Python 3.13, `requirements-dev.txt`). DB fully seeded: vocab + survey +
  upgrade_projects + **`scripts.load_calibration`** (6 anchor archives
  scored) + mipibu harvested (`oai_dc` + `oai_ead` = 1016 records).
  - Start: **`app.bat start`** (or `stop`/`restart`/`status`) →
    `http://127.0.0.1:9000`. Runs `python wsgi.py` (a `__main__` block
    added this session — loads `.env`, `use_reloader=False` so the script
    tracks a single PID). `.venv\Scripts\python -m flask run` still works.
    `DATABASE_URL` is left unset so it defaults to an absolute path under
    `instance/` — a relative `sqlite:///instance/...` fails with "unable to
    open database file".
- **Earlier cloud-sandbox notes (kept for anyone working from there):** a
  dev server may run at `http://localhost:5001` via
  `DATABASE_URL=sqlite:////home/user/workspace/brasil-archives/instance/brasil_archives.db FLASK_APP=wsgi.py flask run --port 5001 --no-reload`,
  using system `flask` (no venv in the sandbox).

### mipibu

- **HEAD:** `3e00a55` on `main`. Clean.
- **Live URL:** `https://mipibu.from-bottom-to.top`
- **OAI-PMH endpoint:** `/oai` — live, 508 case records + ~5000 documents + 1 fonds record.
  - Supports `oai_dc` + `oai_ead`
  - Verifier: `bash scripts/verify_oai_live.sh` — last run 10/10 OK
- **Federation JSON API:** `/api/{health,schema,records,records/<id>}` — live.

### povos-indigenas-rn

- **HEAD:** `817167c` on `main` (2026-08-28 — OAI-PMH provider merged).
- **Deployed** at `https://povos-indigenas-rn.from-bottom-to.top` (returns
  200; the earlier "not deployed" was stale). **Prod predates `817167c`** —
  a routine 3-command pull ships `/oai` (currently 404 live).
- **Tests:** 134 pytest (100 + 34 OAI). `qa_test.py` clean.
- **`/oai`** — OAI-PMH 2.0, `oai_dc`, 145 records / 7 kinds. Design:
  `docs/oai-pmh-povos.md`. Built from spec, not copied from mipibu (mipibu's
  `app/oai/` was only on its `origin/main`).
- **Handoff doc:** `docs/HANDOFF-2026-08-26.md`.
- **Data:** 776 KB `data/povos_rn.db` in-repo (intentional at povos's scale).

## 4. Active tracks

### Track A: brasil-archives public UI polish

Primary reference: [`docs/UI-POLISH-PICKUP.md`](../UI-POLISH-PICKUP.md).
**All five tracks landed 2026-08-27.** The per-item checklist is in
[`TODO.md`](../../TODO.md) §1.

| Sub-track | Status | Effort | Commit |
|-----------|--------|--------|--------|
| **Track 1: i18n catalog (real PT translations)** | **✅ Landed 2026-08-27** | — | see `git log` |
| **Track 2: env-gated admin split** | **✅ Landed 2026-08-27** | — | `f60dfe6` |
| **Track 3: home page redesign** | **✅ Landed 2026-08-27** | — | `ed381fd` |
| **Track 4: locale-aware vocab labels** | **✅ Landed 2026-08-27** | — | `a981b60` |
| **Track 5: metadata + favicon + inline-style cleanup** | **✅ Landed 2026-08-27** | — | `ad8c7d7` |

Track 4 was the pilot for the pull-to-cPanel workflow. It shipped and verified live. See §7 for cPanel deploy pattern.

**What's left on Track A:**

1. **Deploy Tracks 1 + 3 to cPanel.** One pull. Track 1 additionally needs
   `pybabel compile -d app/translations` inside the cPanel venv before
   restart (`.mo` files are git-ignored). Exact commands + verify steps in
   `docs/DEPLOY.md` "Routine deploy" + `TODO.md` §1.
2. **Catalog maintenance only** thereafter: when UI strings change, re-run
   `pybabel extract -F babel.cfg -k _l -o messages.pot .` then
   `pybabel update -i messages.pot -d app/translations`, translate the new
   `pt` msgids, `pybabel compile`. The `wrap_i18n.py` codemod (§5 item 2)
   was **not built** — Track 1's wrapping was done by hand; the codemod
   only pays off if a future track adds many new templates.

### Track B: povos-indigenas-rn OAI-PMH endpoint

Primary reference: `povos-indigenas-rn/docs/OAI-PMH-PICKUP.md`.

**Status:** deferred until user has appetite. Estimated 1.5 days for `oai_dc` MVP, 3 days for full parity with mipibu's `oai_ead` support. **Rule of three applies:** don't extract mipibu's `app/oai/` package prematurely — get povos working with its own copy first, then find the real seams between the two.

**Trigger to resume:** user says "get povos federating" or similar. Once complete, that unblocks registering povos in `brasil-archives` as the second `upgrade_project`.

### Track C: povos-indigenas-rn as an upgrade project

Primary reference: [`docs/integrations/povos-indigenas-rn.md`](../integrations/povos-indigenas-rn.md) (brasil-archives side) + [povos INTEGRATION.md](https://github.com/stevewil/povos-indigenas-rn/blob/main/docs/INTEGRATION.md) (povos side).

Blocked on: (a) povos deploy at `povos-indigenas-rn.from-bottom-to.top`, and (b) povos shipping federation JSON endpoints. Then it's a ~2-hour mechanical integration: seed the composite archives row, add the YAML, load. See §8 for the specific gotchas (source_archive row, delivery_status vocabulary) — the integration doc has the fix baked in as a seed script.

### Track D: Package extraction (mipibu's `app/oai/` → reusable package)

Blocked on Track B. **Do not extract with N=1 caller.** Wait until povos has its own working OAI endpoint, then find the real seams between the two implementations. See `povos-indigenas-rn/docs/HANDOFF-2026-08-26.md` §"Package extraction plan" for the suggested `CorpusAdapter` shape.

## 5. UI polish: token-efficient tooling proposal  *(historical — all 5 tracks are done)*

**Context:** During the 2026-08-27 session the user asked why UI polish tracks cost so many tokens. The honest answer is that most cost is in reading templates, rewriting them, and reasoning about design decisions — with meaningful savings available from tooling. The user endorsed the proposal.

### The tooling to build before Tracks 1, 2, and 5

**Outcome (2026-08-27):** items 1 (`scripts/dev/session_state.sh`) and 3
(`tests/test_template_hygiene.py`) landed in `f983819`;
`test_no_static_inline_style_attributes` is now un-skipped (Track 5 moved
the inline styles out). Item 2 (`wrap_i18n.py`) was **never built** —
Track 1's string-wrapping turned out small enough to do by hand. Keep the
sketch below only as a reference if a future feature adds many templates.

**1. `scripts/dev/session_state.sh` — session bootstrap digest** ✅ landed

Prints a compact digest of current state so the next session doesn't spend ~5k tokens re-discovering it:

```bash
#!/usr/bin/env bash
# Print current state of brasil-archives for a new agent session.
set -e
cd "$(dirname "$0")/../.."

echo "=== git ==="
git log --oneline -3
git status --short
echo
echo "=== tests ==="
python -m pytest --collect-only -q 2>&1 | tail -5
echo
echo "=== live URL ==="
curl -sS https://brasil-archives.from-bottom-to.top/healthz || echo "  (offline?)"
echo
echo "=== DB counts ==="
DATABASE_URL=sqlite:////home/user/workspace/brasil-archives/instance/brasil_archives.db \
python -c "
from app import create_app
from app.extensions import db
from app.models import Archive, UpgradeProject, AggregatedRecord, HarvestRun
app = create_app()
with app.app_context():
    print(f'archives: {db.session.query(Archive).count()}')
    print(f'upgrade_projects: {db.session.query(UpgradeProject).count()}')
    print(f'aggregated_records: {db.session.query(AggregatedRecord).count()}')
    print(f'harvest_runs: {db.session.query(HarvestRun).count()}')
"
```

**2. `scripts/dev/wrap_i18n.py` — codemod for Track 1** ⬜ not built yet

Walk templates, wrap bare text nodes in `{{ _(...) }}` where safe. Rules:
- Skip URL attributes, class names, id attributes
- Skip nodes inside existing `{{ _() }}` or `{% trans %}` blocks
- Skip nodes matching pure-punctuation regex (`—`, `·`, numbers alone)
- Emit a diff for review before applying; only apply on `--apply`

Estimated savings: 30–50k tokens on Track 1 alone (the ~100 mechanical string wraps).

**3. `tests/test_template_hygiene.py` — grep-based template linter** ✅ landed
(the shipped version reads templates with `encoding="utf-8"` — a Windows
cp1252 fix — and keeps the inline-style test skipped pending Track 5)

```python
"""Guardrails against regressions in template patterns."""
from pathlib import Path
import re

TEMPLATE_DIR = Path(__file__).parent.parent / "app" / "templates"


def _templates():
    return list(TEMPLATE_DIR.rglob("*.html"))


def test_no_direct_label_en_access():
    """After Track 4, templates must use vocab_label() not .label_en."""
    bad = []
    pattern = re.compile(r"\.label_(en|pt)\b")
    for tpl in _templates():
        for i, line in enumerate(tpl.read_text().splitlines(), 1):
            if pattern.search(line):
                bad.append(f"{tpl.relative_to(TEMPLATE_DIR)}:{i}: {line.strip()}")
    assert not bad, "Use vocab_label(x) instead of x.label_*:\n" + "\n".join(bad)


def test_no_inline_style_attributes():
    """Detail template inline styles should live in style.css. Guardrail
    for Track 5 — extend the exception list only when there's a genuine
    reason (e.g. dynamic width from a scored value)."""
    bad = []
    for tpl in _templates():
        for i, line in enumerate(tpl.read_text().splitlines(), 1):
            if 'style="' in line and "{{" not in line.split('style="', 1)[1].split('"', 1)[0]:
                bad.append(f"{tpl.relative_to(TEMPLATE_DIR)}:{i}")
    # Uncomment when Track 5 lands. Until then this would fail on
    # detail.html's upgrade-projects section.
    # assert not bad, "Inline styles found — move to style.css:\n" + "\n".join(bad)
```

Estimated savings: prevents ~2-3 round-trips per polish task where I currently have to re-scan templates for regressions.

### Why more can't be automated

- **Editorial judgment cannot be scripted.** Whether a scoring form should be public or admin-only is a design call. Whether "fair use" translates as "uso justo" or "uso legítimo" is an editorial call (project already says "uso justo").
- **Template semantics beyond text nodes.** Some strings sit inside attributes, some are macro parameters. A codemod that gets 80% right is a huge win; getting 100% requires per-case review.
- **Cross-session context reset.** The `session_state.sh` digest helps a lot, but never to zero. The next agent still has to load and reason about the codebase.

### Recommended next-session flow  *(all done — kept as the record of what happened)*

1. ✅ Committed the pending working-tree changes; folded
   `scripts.load_calibration` into `README.md` + `docs/DEPLOY.md`.
2. ✅ Track 5 (`ad8c7d7`) → Track 2 (`f60dfe6`) → Track 3 (`ed381fd`) →
   Track 1 (`2ec138b`/`1089de6`/`bce54e3`, done by a subagent).
3. ✅ `app.bat` (`4f59d27`).

## 6. Local dev quickstart

### brasil-archives (Windows workstation — the primary checkout)

```powershell
cd c:\DEV\brasil-archives
# one-time: py -m venv .venv ; .venv\Scripts\python -m pip install -r requirements-dev.txt
# .env holds FLASK_APP, a real SECRET_KEY, FLASK_RUN_PORT=9000; DATABASE_URL left unset

$env:FLASK_APP="wsgi.py"
.venv\Scripts\python -m flask db upgrade
.venv\Scripts\python -m scripts.load_vocabularies
.venv\Scripts\python -m scripts.load_survey
.venv\Scripts\python -m scripts.load_upgrade_projects
.venv\Scripts\python -m scripts.load_calibration          # 6 anchor archives (now in README/DEPLOY too)
.venv\Scripts\python -m scripts.harvest --project mipibu                 # optional
.venv\Scripts\python -m scripts.harvest --project mipibu --format oai_ead

.\app.bat start                            # http://127.0.0.1:9000 (also: stop | restart | status)
.venv\Scripts\python -m pytest             # 170 passed, 4 skipped
```

`app.bat` runs `python wsgi.py` with `use_reloader=False`, so it's one PID
and `app.bat stop`/`restart` are reliable. If you instead use
`.venv\Scripts\python -m flask run`, the debug reloader spawns a parent +
child — kill both. Track 1 note: `.mo` catalogs are git-ignored; if you
pull translation changes, run `.venv\Scripts\pybabel compile -d app/translations`.

### brasil-archives (cloud sandbox — secondary)

```bash
cd /home/user/workspace/brasil-archives
DATABASE_URL=sqlite:////home/user/workspace/brasil-archives/instance/brasil_archives.db \
  FLASK_APP=wsgi.py flask run --port 5001 --no-reload
curl -s http://localhost:5001/healthz
python -m pytest -q
```

**In the sandbox, do not use `.venv/bin/flask`** — no venv exists there. Use system `flask`.

### povos-indigenas-rn (sandbox)

```bash
cd /home/user/workspace/povos-indigenas-rn
python qa_test.py            # legacy sweep — 146 lines, still canonical smoke
python -m pytest -q          # 100 tests, ~0.4s
```

## 7. cPanel pull workflow (verified 2026-08-27)

**All three apps deploy the same way.** No build step, no Node, no background workers.

### Deploying a new commit

1. In sandbox / local: `git commit && git push origin main` (uses `api_credentials=["github"]`).
2. On cPanel terminal:
   ```bash
   cd ~/<app-dir>   # e.g. ~/brasil-archives or the mipibu path
   git fetch origin
   git pull origin main
   ```
3. Restart Passenger:
   - `touch tmp/restart.txt`  **or**
   - cPanel UI → **Setup Python App → <app> → Restart**
4. Verify:
   ```bash
   curl -s https://<app>.from-bottom-to.top/healthz
   ```

### Verified Track 4 pull (2026-08-27)

On the live site after pull + restart:

```
$ curl -s https://brasil-archives.from-bottom-to.top/healthz
{"app":"brasil-archives","status":"ok","version":"0.1.0"}

$ curl -s https://brasil-archives.from-bottom-to.top/archives/ | \
  grep -oE 'Federal university|State court' | sort -u
Federal university
State court

$ curl -s 'https://brasil-archives.from-bottom-to.top/archives/?lang=pt' | \
  grep -oE 'Universidade federal|Tribunal de justiça' | sort -u
Tribunal de justiça
Universidade federal
```

**If PT and EN cross over after a pull, the Passenger restart didn't take.** Repeat step 3.

### Migration workflow (when tracks add schema)

None of the deferred tracks require migrations. If a future track does:

1. In sandbox: `flask db migrate -m "..."` → review the generated migration → `flask db upgrade` locally → tests → commit.
2. On cPanel after `git pull`: `flask db upgrade` before restarting Passenger.

## 8. Gotchas discovered this session

### Adding povos as an upgrade_project

Attempted 2026-08-27. **Blocked, deferred**, but the analysis is preserved:

- `delivery_status` is editorially enforced as `in-development | beta | stable | deprecated`. No `scaffold` option.
- `source_archive_id` on `upgrade_projects` is NOT NULL. Every upgrade project must reference a row in `archives`.
- **The archives table has no row for povos's source.** The Nordeste digital archives survey doesn't include an Indigenous-history archive. Povos is *both* an archive of assembled evidence *and* the tool that presents it — but until it's cataloged as an archive, it can't be registered as an upgrade project.
- **Recommended path when the time comes:**
  1. Add an `archives` row: `name="Povos Indígenas do RN — corpus"`, `institutional_type=research-project`, `home_state_code=RN`, `no_digital_content=False`, `canonical_url=<future deploy URL>`.
  2. Once povos has an OAI-PMH endpoint working (Track B), add `configs/upgrade_projects/povos-indigenas-rn.yaml` (see `configs/upgrade_projects/mipibu.yaml` for shape).
  3. Run `python -m scripts.load_upgrade_projects` — idempotent, upserts by slug.
  4. Pull to cPanel. Counter goes 1 → 2 legitimately.
- **Handoff doc explicitly says:** register only after `/oai` works. That's still the right call.

### Sandbox cloud browser can't reach `localhost:5001`

Cloud browser sessions are isolated from the sandbox's localhost bindings. When needing to see rendered HTML, use `curl` from `bash` instead — the tool actually needs is a headed browser only for interaction, not for viewing static output.

### Server started with `--no-reload` doesn't pick up Python changes

Templates auto-reload, but changes to `app/__init__.py` (like registering a new Jinja global) require an explicit restart. If you see `UndefinedError: 'foo' is undefined` after adding a global, restart the server:

```bash
pplx-tool start_server <<'JSON'
{"command": "bash /tmp/ba-start.sh", "project_path": "/home/user/workspace/brasil-archives", "port": 5001, "log_file": "/tmp/ba.log"}
JSON
```

with `api_credentials=["pplx-tool:start_server"]`.

### pybabel extraction must run from repo root, with `-k _l`

`babel.cfg` uses the pattern `[jinja2: app/templates/**.html]`. Running `pybabel extract` from `app/` misses the template files silently. Always run from repo root. Also pass **`-k _l`** — Python-side strings use `flask_babel.lazy_gettext as _l`, which is not a default Babel keyword, so without it the flash messages and `page_title=` strings are dropped:
`.venv\Scripts\pybabel extract -F babel.cfg -k _l -o messages.pot .`

### Flask-Babel caches the resolved locale for the app-context lifetime

In tests, the `app` fixture holds one app context open for the whole test, and Flask-Babel resolves the locale once and caches it there. So a single test function **cannot** assert both `?lang=en` and `?lang=pt` — the second request still renders the first locale. Split EN/PT assertions into separate test functions (see `tests/test_i18n_catalog.py`, `tests/test_app.py::test_og_locale_*`). Live curl against a real server is unaffected.

### `messages.pot` and `.mo` are git-ignored — deploy compiles on the host

`app/translations/**/*.mo` and `messages.pot` are in `.gitignore`. A plain `git pull` never refreshes the compiled catalogs, so the cPanel deploy runs `pybabel compile -d app/translations` inside the venv when a pull touched `app/translations/**/*.po`. Documented in `docs/DEPLOY.md` (prereqs + routine deploy).

## 9. What to do first when you take over

1. Read this document, then `TODO.md` (ordered agenda) and `DESCRIPTION.md`
   (app overview) at the repo root.
2. Orient on the Windows workstation:
   ```powershell
   git log --oneline -6
   git status --short
   .venv\Scripts\python -m pytest -q 2>&1 | Select-Object -Last 3   # expect 170 passed, 4 skipped
   curl -s https://brasil-archives.from-bottom-to.top/healthz
   ```
   (`scripts/dev/session_state.sh` also works but is written for the cloud
   sandbox paths.)
3. **Report to the user on the next TODO items** — walk `TODO.md` top to
   bottom and surface the first actionable ones with a one-line status
   each, then let the user pick. As of the end of the UI-polish session
   the live front of the queue is:
   - **Deploy Tracks 1 + 3 to cPanel** (`TODO.md` §1 tail) — one pull +
     `pybabel compile` for Track 1. This is the only thing blocking "UI
     polish fully shipped."
   - **`TODO.md` §0** — decide the `size_unit_note` facet (add a column /
     `facet_values` entry, or document it as YAML-only in
     `algorithm-v1.md`). The other §0 items are done.
   - **`TODO.md` §5 — Pass 3 scoring** (score ~12–15 more archives; the
     actual research work) and **§6 infra** (quarterly probe, scheduled
     harvest) are the larger deferred bodies of work.
   - **Track B / C / D** (povos federation) stay parked until the user
     says "get povos federating."
4. Do not start Track B (povos OAI) or Pass 3 scoring without an explicit
   go-ahead — both are multi-session.

## 10. Key files by concern

**When you need to…**

| ...get the app overview | `DESCRIPTION.md` (repo root) |
| ...see the ordered agenda | `TODO.md` (repo root) |
| ...understand the scoring algorithm | `docs/algorithm-v1.md`, `docs/adr-0001-two-axis-aggregation.md` |
| ...understand federation | `docs/federation-v1.md`, `docs/scenario-driven-federation-model.md` |
| ...understand the data model | `docs/schema-v1.md`, `app/models/*.py` |
| ...understand harvest | `docs/harvest-design.md`, `app/services/harvest.py`, `app/services/oai_client.py` |
| ...add an upgrade project | `configs/upgrade_projects/mipibu.yaml`, `scripts/load_upgrade_projects.py` |
| ...work on public UI | `docs/UI-POLISH-PICKUP.md`, `app/templates/`, `app/static/style.css` |
| ...update translations | `app/translations/pt/LC_MESSAGES/messages.po`, `babel.cfg`, §8 gotchas; controlled-vocab labels are separate (`app/i18n.py` `vocab_label`) |
| ...run the app locally | `app.bat` (start/stop/restart/status), `wsgi.py` `__main__` block |
| ...work on povos OAI | `povos-indigenas-rn/docs/OAI-PMH-PICKUP.md`, `mipibu/app/oai/`, `mipibu/docs/oai-pmh-design.md` |
| ...deploy brasil-archives | `docs/DEPLOY.md` (or this document §7) |
| ...deploy povos | `povos-indigenas-rn/docs/DEPLOY.md` |
| ...integrate povos as an upgrade project | `docs/integrations/povos-indigenas-rn.md` (this side) + `povos-indigenas-rn/docs/INTEGRATION.md` (that side) |

## 11. Anti-patterns — do not do these

- **Don't extract mipibu's `app/oai/` into a package before povos has its own working OAI endpoint.** Rule of three. See §4 Track D.
- **Don't push directly to cPanel.** Always through GitHub. §7.
- **Don't send Wasabi keys over chat.** They stay on the user's workstation.
- **Don't dive into a big multi-file rewrite without a plan committed to the repo.** This session started to do a full UI polish, hit the token wall, backed out, and committed the plan instead. That plan (`docs/UI-POLISH-PICKUP.md`) is now the reason Track 4 could ship cleanly.
- **Don't scope-creep across tracks.** Each track is small and independent for a reason.
- **Don't confuse "Track 2" in different docs.** Phase 3 Track 2 (harvest + aggregated_records) — `docs/handoff/2026-08-26-track-2-start.md`. UI-Polish Track 2 (admin split) — `docs/UI-POLISH-PICKUP.md`. Both are shipped now; the distinction still matters when reading older notes.
- **Don't re-run `pybabel init` on Track 1's catalogs.** `init` overwrites. To pick up new/changed strings use `pybabel update`, then translate the fuzzy/empty `pt` msgids, then `compile`. See §8.
- **Don't set `BRASIL_ARCHIVES_ADMIN=1` on the public cPanel host.** That un-gates the scoring forms, facet editor, and `/harvest`. It belongs only on an internal/operator deployment.

---

**Last verified:** 2026-08-27 (UI-polish session — `main` at `4f59d27`,
170 passed / 4 skipped, tree clean). If more than a week old, run the
checklist in §9 to see what's drifted.
